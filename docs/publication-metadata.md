# Publication metadata for PUBLIC-003

This file records proposed GitHub metadata before a repository exists. No GitHub API, remote, or push is used by this phase.

## Repository description

`Codex Reconnecting 1/5 behind Clash Verge System Proxy on macOS · Codex 重新连接`

## Topics

Use no more than these 13 topics:

`codex`, `openai-codex`, `clash`, `clash-verge`, `clash-verge-rev`, `mihomo`, `macos`, `proxy`, `system-proxy`, `reconnecting`, `websocket`, `http-proxy`, `codex-proxy`

## Search positioning

The public text should describe the real symptom—Codex Reconnecting, slow Codex connection, browser/ChatGPT working while Codex does not—and the bounded solution: read the current macOS System Proxy and pass proxy variables only to the Codex process tree.

Do not claim guaranteed Google, ChatGPT Search, Claude, DeepSeek, or other search placement. Do not use hidden keyword blocks.
